这是在 67 服务器上跑的测试结果，


sysbench 在自己笔记本的 wsl 虚拟机上
mysql server 在厦门大学 67 服务器上（2个 numa node）

innodb buffer pool 2G

每次测得的跨节点相比于本地节点的 tps 竟然更高，不知道是因为什么（怀疑是多次压测，出现热缓存的原因）

只有 20 多的 tps ，大概率是因为网络带宽的问题，造成p99延迟很大，从而影响 tps 性能（后面测试，会把 p99 的数据也呈现出来）